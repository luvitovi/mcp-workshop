**Instalacion de librerias**:

```bash
pip install pypdf sentence-transformers psycopg2-binary pgvector
```

**Vectorizacion de documentos**:

Para vectorizar los documentos, ejecutar:

```bash
python pdf_vectorizer.py ./pdfs/
```

